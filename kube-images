kubectl get pods --all-namespaces | grep registry
docker images accounts

REPOSITORY          TAG       IMAGE ID       CREATED          SIZE
accounts            latest    a1b2c3d4e5f6   2 minutes ago    148MB
